{
  "name": "medela-concertedly",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=client\" \"npm run dev --workspace=server\"",
    "build": "npm run build --workspace=client",
    "start": "npm run start --workspace=client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "turbo": "^2.10.12"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
